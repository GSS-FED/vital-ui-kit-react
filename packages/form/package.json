{
  "name": "@vital-ui/react-form",
  "version": "0.7.0",
  "description": "",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:build": "rm -rf dist",
    "build:esm": "cross-env BABEL_ENV=esm babel src --out-dir dist --copy-files",
    "build:flow": "flow-copy-source src dist",
    "build": "npm-run-all clean:build --parallel build:esm build:flow"
  },
  "author": "",
  "peerDependencies": {
    "react": "^16",
    "react-dom": "^16",
    "styled-components": "3"
  },
  "license": "ISC",
  "dependencies": {
    "@vital-ui/react-icon": "^0.7.0",
    "@vital-ui/react-tooltip": "^0.7.0",
    "@vital-ui/react-utils": "^0.7.0",
    "polish": "0.2.3",
    "react-click-outside": "3",
    "resize-observer-polyfill": "1.5.0",
    "@vital-ui/react-button": "^0.7.0"
  },
  "devDependencies": {
    "flow-bin": "^0.74.0"
  }
}
