{
  "name": "@vital-ui/react-tooltip",
  "version": "0.9.0-y.3",
  "description": "",
  "main": "cjs/index.js",
  "module": "dist/index.js",
  "sideEffects": [
    "*.css"
  ],
  "scripts": {
    "clean": "rm -rf dist lib",
    "copy": "copyfiles src/*.css dist -u 1 && copyfiles src/*.css lib -u 1",
    "compile": "tsc && tsc --module commonjs --outdir cjs && yarn run copy"
  },
  "author": "",
  "peerDependencies": {
    "react": "^16",
    "react-dom": "^16",
    "styled-components": "*"
  },
  "license": "ISC",
  "dependencies": {
    "@vital-ui/react-theme": "^0.9.0-y.3",
    "classnames": "^2.2.6",
    "rc-trigger": "2.6.2"
  },
  "devDependencies": {
    "copyfiles": "^2.0.0",
    "typescript": "^3.0.1"
  }
}
